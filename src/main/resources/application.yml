spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nutricao_esportiva
    username: postgres
    password: 1y5h8j
    initialization-mode: always
    initialize: true
    continue-on-error: true

  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
        jdbc:
          lob.non_contextual_creation: true

oauth-clients:
  projeto_mvp_app:
    client: projeto_mvp_app-client
    secret: projeto_mvp_app-secret